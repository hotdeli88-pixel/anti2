[project]
name = "anti2-workspace"
version = "0.1.0"
description = "Monorepo workspace for anti2 projects"
requires-python = ">=3.11"

[tool.uv.workspace]
members = [
    "apps/*",
    "apps/*/backend",
    "packages/*",
]
